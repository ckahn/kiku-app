# AGENTS.md

Follow the instructions in `CLAUDE.md`.

For any code changes, also follow all rules in or under `~/.claude/rules/common`
and all rules in or under `~/.claude/rules/typescript`, recursively including all
subfolders and files beneath those directories.

If there is a conflict, `AGENTS.md` takes precedence.
