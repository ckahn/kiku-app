# SECURITY: block postinstall scripts (primary supply-chain attack vector)
ignore-scripts=true

# Note: "allow-git" and "min-release-age" are not recognized npm config keys
# and have no effect. Git-source and release-age controls require external
# tooling (e.g. Socket.dev, Snyk) if needed.
